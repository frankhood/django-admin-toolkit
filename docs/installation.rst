============
Installation
============

At the command line::

    $ easy_install django-admin-toolkit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-toolkit
    $ pip install django-admin-toolkit
