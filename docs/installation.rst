============
Installation
============

At the command line::

    $ easy_install django-admin-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-utils
    $ pip install django-admin-utils
